<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.cargram.app.home.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home"
        xmlns:tools="http://schemas.android.com/tools"/>

    <fragment
        android:id="@+id/searchFragment"
        android:name="com.cargram.app.search.SearchFragment"
        android:label="Search"
        tools:layout="@layout/fragment_search"
        xmlns:tools="http://schemas.android.com/tools"/>

    <fragment
        android:id="@+id/createPostFragment"
        android:name="com.cargram.app.create.CreatePostFragment"
        android:label="New Post"
        tools:layout="@layout/fragment_create_post"
        xmlns:tools="http://schemas.android.com/tools"/>

    <fragment
        android:id="@+id/reelsFragment"
        android:name="com.cargram.app.reels.ReelsFragment"
        android:label="Reels"
        tools:layout="@layout/fragment_reels"
        xmlns:tools="http://schemas.android.com/tools"/>

    <fragment
        android:id="@+id/profileFragment"
        android:name="com.cargram.app.profile.ProfileFragment"
        android:label="Profile"
        tools:layout="@layout/fragment_profile"
        xmlns:tools="http://schemas.android.com/tools"/>

</navigation>
